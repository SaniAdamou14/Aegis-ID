title = "Aegis-ID gitleaks config"

[extend]
useDefault = true

# Also run in CI on every push and PR — see .github/workflows/ci.yml.
# Local pre-commit hook (optional):
#   gitleaks protect --staged -v --config .gitleaks.toml
